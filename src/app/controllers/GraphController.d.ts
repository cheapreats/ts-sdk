/**
 * Controller for the graph.
 */
import { App } from "../App";
export declare class GraphController {
    app: App;
    constructor(app: App);
    query(query: string, variables?: any): Promise<object>;
}
//# sourceMappingURL=GraphController.d.ts.map